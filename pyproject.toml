[project]
name = "dev-toolkit-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.3.1",
    "mypy>=2.0.0",
    "ruff>=0.15.12",
]

[project.scripts]
dev_tlk = "dev_tlk.dev_tlk:cli"
